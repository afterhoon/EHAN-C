#include <stdio.h>
#include <string.h>

int main() {
	char str[30];
	int i = 0;

	printf("문자열 입력>> ");
	fgets(str, 30, stdin);
	
	while (1) {
		if ((str[i]) == '\0') {
			printf("%d \n", i);
			break;
		}
		i++;
	}
	printf("문자열의 길이>> %d \n", i);
	return 0;
}
